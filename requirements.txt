camoufox==0.2.14
flask==3.0.3
ollama==0.3.3
aiohttp==3.10.5
tenacity==9.0.0
h2==4.1.0
hpack==4.0.0
hyperframe==6.0.1
Pillow==11.0.0

# Pin browser-use and its langchain stack to a known-good set
browser-use==0.7.8
langchain==1.2.0
langchain-ollama==0.3.10
langchain-core==0.3.49

# Force modern PyYAML wheel so we avoid the old sdist that fails with cython_sources
PyYAML==6.0.2
